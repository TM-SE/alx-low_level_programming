#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (SUCCESS)
 */
int main(void)
{
	int number;
	number = 0;

	while ( number < 10)
	{
		printf("%d",number);
		number++;
	}i
	printf("\n");
	return (0);
}
